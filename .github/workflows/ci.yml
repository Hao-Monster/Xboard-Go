name: CI
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  dispatch-development:
    runs-on: ubuntu-latest
    steps:
      - name: Start development release
        run: echo "Development release requested; business acceptance is performed by the user."
