# Security Policy

## 支持范围

项目仍处于预发布重构阶段，当前版本不声明生产就绪。安全修复优先应用到最新 `main`；预发布 Tag 仅用于可复现测试。

## 报告漏洞

请使用 GitHub 仓库的 Private vulnerability reporting（如可用）向维护者私下报告。不要在公开 Issue 中披露可利用细节、密钥、真实个人数据或生产配置。

报告应包含受影响提交、最小复现、影响范围和建议缓解措施。维护者会先确认接收和严重度，再协调修复与披露时间。

## 项目安全边界

- 不在仓库中保存生产密钥或真实敏感数据。
- 不允许 Go 主进程执行任意 PHP 或上传脚本。
- 当前运行和迁移仅限本地或隔离测试环境。
- 已知 Critical/High 问题未关闭前，不进入候选发布或生产准备阶段。
